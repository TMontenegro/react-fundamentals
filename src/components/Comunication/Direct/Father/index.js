import React from 'react';
import Son from '../Son/index';

export default (props) => (
  <>
    <Son lastName={props.lastName}>Débora</Son>
    <Son lastName={props.lastName}>Tales</Son>
    <Son lastName={props.lastName}>Valdeluce</Son>
  </>
);
